<h1>Articles</h1>

<p> Qtd de variaveis: <%= ENV.size %></p>
<p> Valor da variavel DEFAULT_PASSWORD: <%= ENV['DEFAULT_PASSWORD'] %></p>

<div id="articles">
  <% @articles.each do |article| %>
    <%= render partial: "article", locals: { article: article } %>
  <% end %>
</div>
